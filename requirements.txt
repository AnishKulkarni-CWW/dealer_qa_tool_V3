# ── Core app ──────────────────────────────────────────────────────────────
streamlit>=1.31          # st.dialog (the QA table "Expand" popup) requires 1.31+
pandas
beautifulsoup4
openpyxl                 # Excel read (dealer panel) + Excel report writing

# ── Advanced QA modules ───────────────────────────────────────────────────
opencv-python-headless   # banner_compare.py; headless build needs no libGL
scikit-image             # banner_compare.py (structural_similarity)
numpy
Pillow>=9.1              # LANCZOS resampling used by the OCR upscale path
pytesseract              # needs the tesseract-ocr apt package (see packages.txt)
pymupdf                  # `import fitz` — master PDF page routing and rendering

# pdf2image was removed: nothing in the codebase imports it (grepped every
# module), and it was the only reason poppler-utils was in packages.txt.

# ── Deliberately NOT installed: PaddleOCR ─────────────────────────────────
# The OCR layer falls back to Tesseract automatically when PaddleOCR is
# absent, and Tesseract is what this app has been tuned against. Between
# wheels and first-use model downloads, paddleocr + paddlepaddle will
# exhaust a Community Cloud container.
